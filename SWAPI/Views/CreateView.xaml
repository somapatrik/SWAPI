<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SWAPI.Views.CreateView"
             Title="CreateView">
    <VerticalStackLayout Spacing="5" Padding="10" VerticalOptions="Center">
        <Entry Placeholder="Planet´s name" Text="{Binding Name}"/>
        <Entry Placeholder="Terrain" Text="{Binding Terain}"/>
        <Entry Placeholder="Population" Keyboard="Numeric"  Text="{Binding Population}"/>
            <Button Text="Create planet" Command="{Binding CreatePlanet}"/>
    </VerticalStackLayout>
</ContentPage>